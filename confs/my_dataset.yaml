defaults:
  - train: 20k
  - dataset: default
  - renderer: default
  - pts_sampler: perspective
  - field: hash3d_anchored
  - shader: sh_shader
  - _self_

dataset_name: AWSIM
case_name: 20230717_loop
exp_name: test

is_continue: false

mode: train

renderer:
  # Appearance embedding to handling unstable lighting conditions
  use_app_emb: true

pts_sampler:
  near: 0.01
  scale_by_dis: true

dataset:
  factor: 1
  bounds_factor: [ 0.25, 4.0 ]

train:
  ray_march_init_fineness: 16

do_test_after_train: false
