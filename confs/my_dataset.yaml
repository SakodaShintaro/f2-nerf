defaults:
  - train: 20k
  - dataset: default
  - renderer: default
  - field: hash3d_anchored
  - shader: sh_shader
  - _self_

dataset_name: logiee
case_name: 20230609_base_link_logiee

is_continue: false

mode: train

renderer:
  # Appearance embedding to handling unstable lighting conditions
  use_app_emb: true

pts_sampler:
  sample_l: 3.90625e-3   # 1. / 256.

dataset:
  factor: 1
  bounds_factor: [ 0.25, 4.0 ]

train:
  ray_march_init_fineness: 16

do_test_after_train: false
